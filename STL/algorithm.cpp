#include<iostream>
#include<algorithm>
using namespace std;

int main(){
    //1.max(x,y) min(x,y) abs(x)
    //2.swap(x,y)  //交换x,y
    //3.reverse(it1,it2)    将[it1,it2)的元素反转
    //4.next_permutation(it1,it2)  将[it1,it2)的元素进行全排列 当到达全排列的最后一个时会返回false
    //5.fill(it,it2,x)  将[it,it2)的元素填充为x
    //6.sort(it,it2,'cmp()')      排序  对[it,it2)的元素进行排序  cmp()为比较函数       适用于vector,string,deque等排序set,map不行
    //7.lower_bound(it,it2,x)  返回第一个大于等于x的元素的迭代器  适用于vector,string,deque等排序set,map不行
}